/**
 * Offline service worker plugin
 * Keeps your site running while offline.
 * Runs in production only.
 */

import * as OfflinePluginRuntime from 'offline-plugin/runtime';

OfflinePluginRuntime.install();
