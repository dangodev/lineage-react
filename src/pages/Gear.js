/**
 * Gear Page
 * For all your tactical coffee needs
 */

import React from 'react';

import Collection from '../components/Collection';

export default props => (
  <Collection products={props.products} />
);
